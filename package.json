{
    "private": true,
    "type": "module",
    "scripts": {
        "build": "vite build --configLoader native",
        "dev": "vite --configLoader native"
    },
    "devDependencies": {
        "concurrently": "^9.0.1",
        "laravel-vite-plugin": "^3.0.0",
        "vite": "^8.0.0"
    }
}
